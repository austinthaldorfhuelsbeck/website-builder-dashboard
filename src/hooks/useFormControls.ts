interface FormControlsProps {
	submitFunction: () => any;
	deleteFunction: () => any;
}

const useFormControls = ({
	submitFunction,
	deleteFunction,
}: FormControlsProps) => {};

export default useFormControls;
